name: purge-workflow-logs

on:
  schedule:
    - cron: '0 0 * * 0'  # Run weekly on Sunday at midnight UTC
  workflow_dispatch:

permissions:
  actions: write

jobs:
  purge-logs:
    uses: ./.github/workflows/reusable_purge_workflow_logs.yaml
    with:
      retain-days: 7
